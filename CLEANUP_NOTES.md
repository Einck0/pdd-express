# 清理说明（本轮已执行）

## 第一批：已删除

### 1. `get_package.py`
- 原因：与 `PackageService.py` 功能高度重叠，属于旧实现残留。
- 删除收益：避免“双实现并存”带来的维护混乱。

### 2. `getAT.py`
- 原因：只是一次性调试 `anti-content` 的脚本，正式逻辑已在 `PackageService.py` 中。
- 删除收益：减少调试脚本噪音。

### 3. `Utils.py`
- 原因：未接入主流程，且实现风格与当前 SQLite 架构不一致，更像废弃草稿。
- 删除收益：去掉误导性死代码。

## 第二批：已删除

### 4. `get_sub_pass_id.py`
- 原因：Selenium 方案未接入当前主线，且引入额外依赖与维护成本。
- 删除收益：统一获取逻辑，减少技术分叉。

### 5. `cookies.json`
- 原因：仅服务于旧 Selenium 路线。
- 删除收益：清理历史运行产物。

## 第三批：已修改

### 6. `docker-compose.yml`
- 修改内容：
  - 移除 `config.ini` 挂载
  - 改为 `env_file: .env`
  - 启动前执行 `init_db.py`
- 收益：部署方式与新配置方案统一。

### 7. `Dockerfile`
- 修改内容：
  - 启动命令改为先初始化数据库再启动服务
  - 与 `.env` 路线保持一致
- 收益：镜像运行行为更符合当前项目结构。

## 额外说明
本轮清理遵循原则：
- 删除高概率无用且未接入主链路的文件
- 保留主业务文件
- 让部署文件与新的 `.env` + 新 schema 方案一致
